/**
 * Transport creation helpers.
 *
 * In this standalone implementation, transport creation lives inline in
 * McpServerManager (see createHttpTransport). This file is reserved for
 * future transport-level utilities if needed.
 */
export {};
